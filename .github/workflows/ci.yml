name: CI

on: [push]

jobs:
  build:

    env:
       NUMBA_DISABLE_JIT: 1
    
    runs-on: macos-latest
    strategy:
      max-parallel: 5
      matrix:
        python-version: [2.7]

    steps:
    - uses: actions/checkout@v1
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip2 install numpy==1.16.5 scipy
        pip2 install llvmlite pytest pytest-cov codecov
        #pip2 install numba hypothesis  astropy==2.0.16 astromodels codecov 
        pip2 install -r requirements.txt
    # - name: Lint with flake8
    #   run: |
    #     pip install flake8
    #     # stop the build if there are Python syntax errors or undefined names
    #     flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
    #     # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
    #     flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      run: |        
        python -m pytest -vv --cov=pynchrotron/
        codecov --token=95a59626-4d5a-47a2-9263-4fa3a919a1bd
